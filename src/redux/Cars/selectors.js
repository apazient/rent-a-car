export const selectCars = (state) => state.cars.cars;

export const selectPage = (state) => state.cars.page;
export const isLoading = (state) => state.cars.isLoading;
export const selectRes = (state) => state.cars.rest;
